# Maintainer: Sung Pae <self@sungpae.com>
pkgname=fakedeps
pkgver=1
pkgrel=1
pkgdesc="A dummy package that satisfies annoying dependencies without actually installing any files."
arch=('any')
license=('GPL')
groups=('nerv')
provides=(polkit rtkit avahi at-spi2-core gvfs)
conflicts=(polkit rtkit avahi at-spi2-core gvfs)
replaces=(polkit rtkit avahi at-spi2-core gvfs)
